import { SearchPartnerPipe } from './search-partner.pipe';

describe('SearchPartnerPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPartnerPipe();
    expect(pipe).toBeTruthy();
  });
});
